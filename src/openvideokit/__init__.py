"""OpenVideoKit — installable CLI + SSR/agentic server.

`pip install openvideokit` → `ovk serve` runs the FastAPI server on :8000
(serves the editor SPA + stamped HF compositions for <hyperframes-player>).
"""

__version__ = "0.2.0"
